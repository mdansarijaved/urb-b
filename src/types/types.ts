export interface Product {
  id: string;
  name: string;
}

export interface User {
  id: string;
  username: string;
  password: string;
}

export interface shortURL {
  id: string;
  url: string;
  shortUrl: string;
}
